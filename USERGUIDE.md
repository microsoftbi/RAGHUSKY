# RAGUSKY 用户手册

适用版本:0.1.x(脚手架版本)
更新日期:2026-05-25

本手册面向 RAGUSKY 的两类终端使用者:**普通用户(user)** 与 **管理员(admin)**。运维/部署相关内容请见 `02-摄入脚本使用说明.md` 与 `04-切分参数说明.md`。

---

## 1. 系统是什么

RAGUSKY 是一个本地部署的 RAG(检索增强生成)系统。把你的私有文档放进 `Documents/` 文件夹,运行一次摄入脚本,系统就能基于这些文档回答问题,并在答案末尾给出可点击的引用,让你回溯每一句话的来源。

主要特性:

- **离线知识库**:文档只通过 `Documents/` 文件夹 + 摄入脚本进入,**没有在线上传 UI**。
- **带引用回答**:每条助手回复后会列出 `[chunk_id]` 短标签,点击可看到对应原文片段。
- **角色分明**:普通用户只能问答检索;管理员还能管账户、看文档信息、做召回测试、调切分参数。
- **流式输出**:回答按 token 实时流出,不需要等全部生成完才显示。

---

## 2. 登录

1. 打开浏览器访问 `http://localhost:5173`(或运维告诉你的地址),会自动跳到登录页。
2. 输入用户名 + 密码,点击"登录"。
3. 初始管理员账号是 `admin`,密码由部署时的 `.env` 文件中的 `INITIAL_ADMIN_PASSWORD` 决定。普通用户账号需要管理员在后台创建后才能使用。
4. JWT 默认 12 小时过期,过期后系统会跳回登录页,重新登录即可。

**忘记密码**:目前没有"找回密码"功能,联系管理员在"用户管理"页里重置。

---

## 3. 普通用户:问答

登录成功后默认进入 **问答** 页。

### 3.1 提问

- 在底部多行输入框里写问题,**Enter 发送**,**Shift+Enter 换行**。
- 也可以点右下角"发送"按钮。
- 提问时建议尽量贴近文档里出现过的术语,比如直接问"切分参数怎么改",比问"我想调一下那个参数"更容易召回到正确切片。

### 3.2 看回答

- 助手气泡里会显示 `…`,随后 token 逐字流入。
- 答案末尾会出现一排小标签,形如 `fb3dc6a7`,每个标签代表一条引用切片。
- **鼠标点击标签**,会弹出 popover 显示:
  - 来源文件相对路径(如 `03-用户角色与权限.md`)
  - 相似度分数
  - 切片正文片段

如果模型在知识库里找不到相关内容,会直接回复"知识库中没有相关信息。"——这是设计行为,不是 bug,**不会编造**。

### 3.3 多轮追问

- 同一个浏览器标签页里的连续提问会**共享同一个 `session_id`**,后端可以基于历史会话理解上下文。
- 想新开一个干净的会话:刷新页面即可。
- 历史会话目前只在数据库里留存,不在前端展示;后续版本会加"侧边会话列表"。

### 3.4 查看知识库目录

顶部菜单 **知识库文档** 列出当前已入库的所有文档(标题 / 路径 / 切片数 / 入库时间),用来确认"我以为放进去的那篇文档,系统里到底有没有"。

---

## 4. 管理员:后台四件套

管理员登录后,顶部菜单会多出四项:**用户管理 / 文档信息 / 召回测试 / 切分参数**。

### 4.1 用户管理

- **新建用户**:填用户名 + 初始密码 + 角色(`user` / `admin`)。
- **重置密码**:右侧操作列点"重置",输入新密码即可,旧密码不需要。
- **启用/禁用**:用 switch 切换,禁用后该用户立即无法登录(已签发的 token 在过期前仍然有效)。
- **删除**:谨慎使用,会连带删掉该用户的会话与消息记录。

**安全锁**:系统至少保留一个**启用状态的管理员**。删除/禁用/降级最后一个 admin 都会被拒绝,前端会弹错误提示。

### 4.2 文档信息

显示每篇文档的完整元数据:`doc_id`、`source`、`sha256`、`size_bytes`、`mime_type`、`chunk_count`、入库时间。点行可展开查看该文档的前若干切片预览(用于核对解析是否正确)。

如果发现某篇文档没出现,通常是:

1. 扩展名不在支持列表 → 看 `02-摄入脚本使用说明.md`。
2. 摄入脚本还没跑过 / 没跑成功 → 看 `LOG/ingest.log`。
3. 内容与已有文档完全一致 → sha256 命中去重,被记作 `skip(duplicate-content)`。

### 4.3 召回测试

用来**只验证检索质量,不消耗 LLM 调用费用**。

- 输入 query、设置 `top_k`(默认 4)。
- 提交后返回每条切片的 `chunk_id`、`source`、`score`、完整文本。
- 整个过程**不会调用 LLM**,只走 embedding + 向量检索。
- 适用场景:
  - 调完 `chunk_size` 后看新切片粒度合不合适。
  - 对比换不同 query 写法时的召回差异。
  - 排查"为什么 LLM 答不出来"——先确认是检索没召回,还是召回了但生成没用上。

每次调用都会写一行 `retrieval_test` 事件到 `LOG/retrieval.log`,包含操作员、query、top_k、结果列表,便于审计。

### 4.4 切分参数

可调三项:

| 字段 | 默认 | 说明 |
| --- | --- | --- |
| `chunk_size` | 800 | 单切片目标字符数 |
| `chunk_overlap` | 120 | 相邻切片重叠字符数,**必须严格小于** `chunk_size` |
| `splitter` | `recursive` | 切分策略,可选 `recursive` 或 `markdown_aware` |

修改保存后:

- 后端会校验参数合法性,写入 `ingest_config_history` 表(可在页面下方看历史)。
- 响应里带 `requires_rebuild=true`,前端给出提示。
- **此时旧文档仍在旧 collection**,新查询会走新 collection,而新 collection 是空的。必须在 `backend/` 下运行:

  ```
  uv run python -m scripts.ingest --rebuild
  ```

  才会按新参数把所有文档重新切、重新 embed、写入新 collection。详见 `04-切分参数说明.md`。

---

## 5. 常见问题(FAQ)

**Q1:我把文件丢进 `Documents/` 了,聊天却找不到?**
没跑摄入脚本。RAGUSKY 不会监听文件系统变化,任何新增/修改/删除都需要在 `backend/` 下运行 `uv run python -m scripts.ingest`。

**Q2:Word 文档为什么内容不全?**
当前 `.docx` 解析只抽段落和表格,**丢弃**了图片、文本框、页眉页脚、批注。需要保留这些请把 Word 先另存为 PDF 再放进 `Documents/`。`.doc`(Office 97-2003 旧格式)不支持。

**Q3:为什么有时候答案末尾没有引用 chip?**
两种可能:
1. 模型回答时没按格式写 `[chunk_id]`(LLM 偶尔会偷懒)。
2. 引用的 chunk_id 不在本次召回结果里,被过滤掉了。
通常重新问一遍、或把问题写具体一点就会带上引用。

**Q4:回答是错的怎么办?**
先去 **召回测试** 输入同一个 query,看检索到的切片里有没有正确答案:
- 切片里**有**正确答案 → 是 LLM 没用上,可以试着把问题写得更精确,或者升级生成模型。
- 切片里**没有**正确答案 → 是检索没召回,通常意味着 `chunk_size` 太大/太小、或文档本身没覆盖这个问题。

**Q5:可以问跨文档的综合性问题吗?**
可以,但效果取决于 `top_k`(默认 4)。如果一个问题需要拼 6-8 个切片的信息,可能召回不全。管理员可以在召回测试里临时提高 top_k 验证。

**Q6:删了 `Documents/` 里的文件,库里会自动清掉吗?**
跑下一次摄入脚本时会(默认开启 `--prune`)。想保留库内记录用 `--no-prune`。

---

## 6. 安全与隐私

- 所有问答都会写一行 `query` + 一行 `answer` 到 `LOG/chat.log`,包含用户名、原始问题、完整答案、引用列表。如果你提交了敏感信息,管理员看日志时也能看到。
- 召回测试也会落 `LOG/retrieval.log`,包含管理员账号、query、返回的切片正文片段。
- 这些日志不会上传到任何外部服务,但本地服务器上的人有权限读。

---

## 7. 反馈渠道

发现 bug、想要新功能、或者觉得某段文档写得不清楚,直接告诉系统管理员;管理员可以在仓库的 `docs/features/` 下登记需求,排进下一个版本。
